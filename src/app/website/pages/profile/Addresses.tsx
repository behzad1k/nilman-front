import {AddressRow} from './addressRow.tsx';

export const Addresses = () => {
  return (
    <section className="addressSection">
      <AddressRow />
      <AddressRow />
      <AddressRow />
    </section>
  );
};
